
name: 'Terraform'

on:
  push:
    branches: [ "master" ]
  pull_request:

permissions:
  id-token: write
  contents: read

# env:
  # ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
  # ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
  # ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
  # ARM_USE_OIDC: true

jobs:
  terraform:
    name: 'Terraform'
    runs-on: ubuntu-latest
    environment: production

    # Use the Bash shell regardless whether the GitHub Actions runner is ubuntu-latest, macos-latest, or windows-latest
    defaults:
      run:
        shell: bash

    steps:
    # Checkout the repository to the GitHub Actions runner
    - name: Checkout
      uses: actions/checkout@v4

    - name: Azure Login
      uses: azure/login@v2
      with:
        client-id: ${{ secrets.AZURE_CLIENT_ID }}
        tenant-id: ${{ secrets.AZURE_TENANT_ID }}
        subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
        # auth-type: IDENTITY

    # Install the latest version of Terraform CLI and configure the Terraform CLI configuration file with a Terraform Cloud user API token
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v1

    # Initialize a new or existing Terraform working directory by creating initial files, loading any remote state, downloading modules, etc.
    - name: Terraform Init
      run: terraform init

    # Checks that all Terraform configuration files adhere to a canonical format
    #- name: Terraform Format
    #  run: terraform fmt -check

    # Generates an execution plan for Terraform
    - name: Terraform Plan
      run: terraform plan -input=false

      # On push to "master", build or change infrastructure according to Terraform configuration files
      # Note: It is recommended to set up a required "strict" status check in your repository for "Terraform Cloud". See the documentation on "strict" required status checks for more information: https://help.github.com/en/github/administering-a-repository/types-of-required-status-checks
    - name: Terraform Apply
      if: github.ref == 'refs/heads/master' && github.event_name == 'push'
      run: terraform apply -auto-approve -input=false

    # 7. Extract the storage account name cleanly using step outputs
    - name: Get Storage Account Name
      id: get_sa_name
      run: |
        # Strip all formatting and isolate the name: modelinfoaccount
        CLEAN_NAME=$(terraform output -raw storage_account_name | grep -o 'modelinfoaccount' | head -n 1 | tr -d '\r\n ')
        echo "storage_account=$CLEAN_NAME" >> $GITHUB_OUTPUT

    # 8. DIAGNOSTIC RUN: Find out exactly what Azure sees
    - name: Diagnose Azure Permissions
      uses: azure/cli@v2
      env:
        TARGET_ACCOUNT: ${{ steps.get_sa_name.outputs.storage_account }}
        RESOURCE_GROUP: "myResourceGroup"
      with:
        inlineScript: |
          echo "================ DIAGNOSTICS START ================"
          
          echo "1. Checking active logged-in account profile:"
          az account show --query "{user:user.name, subscription:name, tenantId:tenantId}" --output table
          
          echo "2. Testing if we can list ALL storage accounts in this subscription:"
          az storage account list --query "[].{name:name, resourceGroup:resourceGroup}" --output table
          
          echo "3. Checking if we can read the target account metadata directly:"
          # This will safely test the exact command failing right now
          az storage account show --name "$TARGET_ACCOUNT" --resource-group "$RESOURCE_GROUP" --query "{name:name, location:location}" --output table || echo "Failed to read storage account metadata."

          echo "================= DIAGNOSTICS END ================="

